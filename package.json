{
  "name": "packcode",
  "version": "0.6.0",
  "private": true,
  "description": "Stop wasting prompts. Get sharper answers from any AI.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
